# Brand audit — 2026-09-06

Against `docs/brand-bible.md` (Get better. / D1 forest / warm coach / active soft closes).

## Findings fixed in this pass

### Soft closes
- Removed the systemic **“The intended model…”** product-spec pattern across Learn articles.
- Rewrote soft closes to warm coach **“IOFitness is built to / around…”** lines.
- No remaining `being designed` or `intended model` in `content/articles/`.

### Titles / openings
- Missed workout: calmer title; calendar vs trained framing in plain English.
- Progressive overload: title + human opening (stuck dumbbells), not a definition memo.
- Personalization: dropped punchy “Actually.”
- Adaptive training: situation-first opening.
- Exercise selection H2: “How a good plan chooses exercises” (not “How IOFitness should…”).

### Homepage
- “Why IOFitness exists” + active built-to framing (no “we’re building…” drift).

### Visual (D1)
- **Fraunces / `font-display` reserved for the IOFitness wordmark** in the header.
- Homepage, Learn index, article H1/H2, Sources, and 404 headings moved to `font-sans`.

## Still good / no change needed
- Brand line already `Get better.` in hero + `siteConfig`.
- Naming is IOFitness (one word).
- No diagnosis / guarantee claim problems found in the sample.
- Banned jargon table mostly clean.

## Follow-ups (not in this pass)
- Deeper body pass on older authority pieces for residual memo tone (`fatigue budget`, dense S&C headings).
- Optional: OG image still uses large display type for “Get better.” — confirm whether brand line may use Fraunces as lockup partner or stay sans.
- App (`iofit`) visual alignment to D1 tokens — separate pass.
